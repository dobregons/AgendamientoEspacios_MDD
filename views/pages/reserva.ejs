<div class="container">
    <div class="row">
        <div class="ui-widget">
            <label>Selecciona un Espacio: </label>
            <select id="combobox">
                <option></option>
                <% espacios.forEach(function(row){ %>
                    <option value="<%= row.id %>">
                        <%= row.nombre %>
                    </option>
                    <% }) %>
            </select>
        </div>
    </div>
    <!--Código calendario Dilver-->
    <h1>CALENDARIO</h1>
</div>